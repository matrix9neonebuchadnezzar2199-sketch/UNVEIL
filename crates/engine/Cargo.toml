[package]
name = "unveil-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Static probe, detectors, and safe adapters. Input bytes are never executed."

[dependencies]
unveil-contracts = { path = "../contracts" }
flate2 = "1"
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.10"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
tempfile = "3"
